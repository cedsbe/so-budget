# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

so-budget is a budget application. The repository is currently a blank slate: only a
`README.md` and a `.gitignore` exist, no source code has been written yet. The `.gitignore`
is the Go community template, suggesting Go is the intended language, but no `go.mod` or
other scaffolding has been created yet.

There are no build, lint, or test commands to document yet — update this file once the
project structure (language tooling, module layout, test setup) is established.
